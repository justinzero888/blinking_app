# TODO-2026-03-19

## Bugs for Current blinking notes

- Card sharing does not contain any content, just one line of text "coming from Blinking " Card sharing should contain the content of the card, such as the title, description, and backgroundimage.
- The jar of emoji when empty, the AI is inactive. It should be active by default and show the AI flow. 
- For emoji jar, never allow emoji fall out of the jar. Make the jar more realistic and allow emoji to be over lapping each other. 


## New Features for Blinking Notes

- For AI: 1) be able to change AI image by allow user to upload image or use AI to generate image. 2) Give AI permission to access user's notes to have meaningful and relevant conversation. 3) user can specify the date range of the notes. 4) AI can be used to summarize the notes in the date range, and the summary can be saved as a note. 5) Allow save part of the conversation as a note. 

- For Habit: 1) Allow user to upload image/icon for the habit. 2) define the file format for the habit list so user can import/export the habit list. 

- For Emoji: 1) Allow user to upload image/icon for the emoji. 2) define the file format for the emoji list so user can import/export the emoji list.   

- make sure the language on the app is consistent with the language setting. Do not mix the languages. For example, if the language setting is Chinese, then all the text on the app should be in Chinese. 



