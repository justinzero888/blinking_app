# v1.2.0 — Issues & Defects Register

> **Living document.** Updated with every bug found during dev, UAT, and manual QA.  
> **Fixed issues** are collapsed at the bottom under "Resolved."  
> **Open issues** remain at top in priority order.  
> **Repository:** `/Users/justinzero/ClaudeDev/blink/blinking_app`

---

## Open Issues

### 🔴 P0 — Blocking Ship

| ID | Title | Symptom | Root Cause | Affected | Status |
|----|-------|---------|------------|----------|--------|
| B1 | AI Daily Reflection shows English in Chinese mode | Persona name "Kael" (not "楷迩"), lens questions all English when locale is ZH | `AiPersonaProvider._load()` always loaded `style.name` + `style.persona(false)`. `LensSet` seeded once in SQLite and never re-localized. `reSeedLensesForLocale()` hardcoded `setActiveLensSet` to Kael. `_onLocaleChanged()` hook missing from onboarding screen. Fixed: locale-aware `_load()`, lens re-localization on toggle, active lens preservation, onboarding hook added. | iPhone, iPad, Android | ✅ Resolved May 27 |
| B2 | "自定义" template shows Chinese name in EN locale | 9th template "自定义" appears in picker, tapping does nothing | Dead `copyBuiltInTemplate` method + orphaned DB artifact from early dev. Fixed: removed dead code, one-time DB cleanup of orphaned non-built-in templates, getter filter as safety net. | iPhone, iPad, Android | ✅ Resolved May 27 |

### 🟡 P1 — Must Fix Before Ship

| ID | Title | Symptom | Root Cause | Affected | Status |
|----|-------|---------|------------|----------|--------|
| V1 | Voice notification only works in-app, not background | Routine reminders fire as silent notifications when app is closed. Voice speaks only when app is in foreground or the user taps the notification to open the app. | `flutter_tts` is a foreground-only TTS engine. The Flutter engine isn't running when the app is backgrounded/killed, so `speak()` cannot execute. Platform-level background TTS requires iOS `AVSpeechSynthesizer` in a background task or Android `ForegroundService` with `TextToSpeech` — neither is implemented. | iPhone, iPad, Android | 🔲 Open — deferred to v1.3.0 (4h) | |

### 🟠 P2 — Should Fix

| ID | Title | Symptom | Root Cause | Affected | Status |
|----|-------|---------|------------|----------|--------|
| — | *(none yet)* | | | | |

### ⚪ P3 — Known, Deferred

| ID | Title | Symptom | Root Cause | Affected | Status |
|----|-------|---------|------------|----------|--------|
| D1 | No live preview in CardBuilderSheet | User can't see how card will look before saving. Only template thumbnails shown. | CardBuilderSheet renders only picker + editor + toggles. `buildPreviewWidget()` is used only during save capture, never shown to user. | iPhone, iPad, Android | Deferred to v1.3.0 |
| D2 | No photo shown in builder | Entry photos inherited from note but not visible in builder | `widget.initialPhotoPath` forwarded to renderer but no preview widget in builder UI | iPhone, iPad, Android | Deferred to v1.3.0 |
| D3 | Can't change/remove photo in builder | Photo locked to entry's first media. No add/remove/swap | Feature not implemented in builder UI | iPhone, iPad, Android | Deferred to v1.3.0 |
| D4 | `copyBuiltInTemplate` method dead code | Method exists in `CardProvider` but never called from any UI | Feature deferred — custom template saving moved to v1.3.0 | All | Deferred to v1.3.0 |
| D5 | No kaishu font | All templates use MaShanZheng (xingshu) instead of kaishu | Need to bundle LXGW WenKai font — documented in `.opencode/skills/blinking-lessons/SKILL.md` TODO | All | Deferred to v1.3.0 |

---

## For Triage (UAT Results Pending)

| ID | Title | Reported By | Platform | Date | Status |
|----|-------|-------------|----------|------|--------|
| U1 | Card preview back button missing Semantics ID | Testing agent | iPhone, iPad | May 27 | ✅ Fixed — added `btn_card_preview_back` |
| U2 | Card builder TextField doesn't auto-focus on iPad | Testing agent | iPad | May 27 | ✅ Fixed — added `autofocus: true` |
| U3 | Maestro incompatible with Android 16 API 36 | Testing agent | Android | May 27 | ⚠️ Known — Maestro 2.6.0 doesn't support API 36. Downgrade emulator to API 35 or wait for Maestro update. |

### UAT Run Results — May 27

| Device | Result |
|--------|--------|
| iPhone 17 Pro (iOS 26.4) | **10/10 ✅** |
| iPad Air 11" M4 (iOS 26.4) | **10/10 ✅** |
| Android Medium Phone API 36 | **BLOCKED** — Maestro incompatibility |

---

## Resolved

| ID | Title | Resolution | Date |
|----|-------|------------|------|
| B3 | Save keepsake fails on iOS — "Save failed" toast | `renderToFile` → `_renderOffscreen` used manual `PipelineOwner` + `element.mount(null, null)` which only works under `TestWidgetsFlutterBinding`, not production `WidgetsFlutterBinding`. Fixed by switching to OverlayEntry + `captureFromKey` with `Positioned(left: -2000)` off-screen rendering. | May 26 |
| B4 | Background image not rendering on Android | `Image.memory(bytes)` decodes asynchronously — `RepaintBoundary.toImage()` captures before decode completes. Fixed: pre-decode via `decodeImageFromList()` → `RawImage` which paints synchronously. | May 26 |
| B5 | Yellow underline under all text on cards | `TextDecoration.underline` inherited from Navigator's Overlay `DefaultTextStyle` theme. Fixed: added `decoration: TextDecoration.none` to all 6 `TextStyle` instances in card renderer. | May 26 |
| B6 | Card content overflows into overlay row | Text area (`Expanded` in `Column`) received full card height without accounting for overlay. Fixed: restructured all 4 layouts to use `Column(Expanded + overlay)` instead of `Stack` siblings. | May 26 |
| B7 | `db_version_test` fails — expects schema 15, code is 16 | Test not updated after v16 migration. Fixed: test now asserts 16. | May 27 |
| B8 | Preview screen stuck on iOS after save | `OverlayEntry.remove()` fails during route transition (concurrent modification of `Overlay._entries`). Fixed: deferred removal via `addPostFrameCallback` + `Positioned(left: -2000)` makes card invisible even if removal fails. | May 26 |
| B9 | "Save failed" with `objective_c.framework` dyld error on iOS | Build artifact cache stale — device-slice binary used for simulator. Fixed: `flutter clean` + `flutter build ios --debug --simulator`. | May 27 |
| B10 | AI Rewrite returns LLM preamble in card text | Prompt asked LLM to "polish" text but didn't forbid explanations. LLM added "Here's the polished version:" prefix and "---" separator. Fixed: prompt updated to "Return ONLY the polished text — no explanations" + `_stripAiPreamble()` safety net removing common LLM boilerplate. | May 27 |
| B11 | Photo not appearing in keepsake cards | `widget.initialPhotoPath` never forwarded to `buildPreviewWidget` in save handler. Also: relative paths like `media/xxx.jpg` not resolved to absolute. Fixed: added `imagePath: photoPath` parameter + `getApplicationDocumentsDirectory()` path resolution. | May 27 |
| B12 | Text overlaps footer area on bamboo template | `textPaddingBottom: 140` insufficient for long welcome notes. Fixed: increased to 200. | May 27 |
| B13 | Corner styles not applied to cards | `cornerStyle` parsed into `_CardConfig` but never read in `_CardRenderWidget.build()`. Fixed: added `ClipRRect` with `borderRadius` based on `cornerStyle` (rounded=24, sharp=0, pill=720). | May 26 |
| B14 | Font family `fontFamily` field unused | `CardTemplate.fontFamily` parsed but never applied to `TextStyle`. Fixed: `_resolveFontFamily()` now maps `serif`→MaShanZheng, `mono`→monospace, `default`→system sans-serif. | May 26 |
| B15 | Text backdrop padding not subtracted from font size calc | `Container(padding: 20)` added 40px of padding to each dimension but `_findOptimalFontSize` used outer constraints. Fixed: subtract 40 from both dimensions when backdrop is active. | May 26 |
| B16 | Decorative motifs nearly invisible | All 6 motifs had very low opacity (7-15%) making them imperceptible on most displays. Fixed: increased opacities (crescent 8%→25%, bamboo 15%→20%, steam 8%→12% with thicker stroke, mountains 8-12%→12-18%, rice paper 3%→7%). | May 26 |
| B17 | 墨韵/素笺/朱砂 bgColor identical | All three used `#F5F0E8` (warm cream with yellow undertone). Fixed: changed to `#F2EFE9` (neutral greige), differentiated 朱砂 to `#FAFAFA`. | May 27 |
| B18 | Seal stamp overlapped center text | `_buildSealStamp()` was a `Center` widget behind text. Fixed: repositioned to `Positioned(right: 80, bottom: 200)`. | May 26 |
| B19 | Mountain silhouettes consumed 50% of card | `height: _cardHeight * 0.5` on `_buildMountainSilhouettes` filled bottom half. Fixed: reduced to 30%. | May 26 |
| B20 | Overlay fonts 3× too large after initial fix | First pass scaled from 24/18/16/14 to 64/44/36/28 (3×). Too aggressive. Second pass reduced to 28/18/18/15. | May 26 |
| B21 | Image darkening insufficient for text readability | 15% black overlay on full-bleed photos made text invisible on dark images. Fixed: gradient vignette 20%→50% darkness. | May 26 |
| B22 | Overlay position formula incorrect | `textPaddingBottom - 60` placed overlay ~360px from bottom instead of ~80px. Fixed: hardcoded `bottom: 80`. | May 27 |
| B23 | Save keepsake button not functioning on all 3 sims | Switched to `renderToFile` for save — `_renderOffscreen` broken on devices. Reverted to OverlayEntry + `Positioned(left: -2000)` which works cross-platform. | May 27 |
| B24 | Demo entries needed for card testing | No entries to create cards from on fresh install. Fixed: seeded 8 demo entries with varied content lengths (1-300+ chars), emotions, and languages (ZH/EN). | May 27 |
| B25 | Onboarding screen 2 outdated | Generic "Notes, Habits, AI" copy didn't differentiate product. Updated to "Everything in My Day" with Unified Calendar, Blinking Notes, Habit Tracking, and Privacy anchor. | May 27 |
| B26 | `kSchemaVersion` / `CLAUDE.md` mismatch | Code at v16, docs said v15. Fixed both. | May 27 |

---

## Scope Boundaries — What NOT to Fix in v1.2.0

| Item | Reason |
|------|--------|
| Custom template saving | Deferred to v1.3.0 — needs photo picker integration |
| Live card preview in builder | P2 UX enhancement, not a regression |
| Kaishu font upgrade | P3 — MaShanZheng is acceptable |
| Photo add/remove/swap in builder | Deferred to v1.3.0 |
| `db_index_test` migration failures (2 tests) | Pre-existing before v1.2.0, unrelated to Keepsake |
