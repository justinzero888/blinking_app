# Blinking Notes Feature List

- Update the daily routine item content, assign default icon to represent the routine type.
- Update Daily Routine List Behavior: For each routine each day, when the routine is completed, the routine is removed from the list and added to the completed list.The completed items turn into a checked icon with checkmark. The checked items are grouped at the bottom of the list in one section. Ensure the daily completed list is not affected by the daily routine list.
- turn daily notes into a "card" that can be stored in a special "folder" and published to social media. "Card" can be generated with templates which can be selected from a dropdown menu. and the template include set of background images, font styles, and font colors.    "Folder" is a special folder that can be used to store daily notes with a special icon and name. 
- the behavior of card generation is as follows: each notes has a "card" button, when clicked, a template selection dialog is shown, when selected, the notes is converted into a card and stored in the special "folder". the card can be published to social media. the card can be edited and deleted. 
- Create default set of templates and folders. Give each template a unique icon and name. Define the template format and allow users to define their own templates.
- Create a new attributes for each note to represent the emotion/feeling of the note. Use emoji icons to represent the emotion/feeling. at the end of each day, the app will calculate the "emoji" of the day based on the emotion/feeling of all notes. The emoji of the day will be displayed in the home screen and each day on the calendar screen.
- create a "jar" to store the emoji of the day. The jar is transparent and all the emoji of the day are displayed in the jar. AI assistant can be used to generate the emoji of the day based on the emotion/feeling of all notes. AI assistant can also be used to generate the jar design based on the emoji of the day. AI assistant can also be used to generate "encouragement" message based on the emoji of the day or the entire content of the emoji. AI assistant can also be used to generate "inspiration" message based on the emoji of the day or the entire content of the emoji. AI assistant can also be used to generate "motivation" message based on the emoji of the day or the entire content of the emoji.
- Automatically generate a "jar" for each year. Create a shelf screen to display all the jars. 
- Update the behavior of AI assistant as follow: save the conversation history of the user and AI assistant to a summarized notes with timestamp and AI assistant icon. The AI assistant notes can be edited and deleted.this activity can be associated with "reflection" notes. 
- Remove "daily summary", "weekly summary", "monthly summary" from the app. Intead creating a visual summary of the day, week, month, year, and life. The summary can be generated based on the content of all notes, daily routines completion and emoji of the day.



