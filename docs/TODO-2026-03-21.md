# TODO-2026-03-21 - goal is to create a complete beta version for public release

- build a branch called "beta-2026-03-21" from "main" branch.
- remove video and record function from the app. 
- remove notification function from the settings.   
- add privacy policy and terms of service to the settings section. policy and terms of service are in BLINKING_APP_PRIVACY_POLICY.md and BLINKING_APP_TERMS_OF_SERVICE.md in the docs folder.  
- Language consistency: make sure the language on the app is consistent with the language setting. Do not mix the languages. For example, if the language setting is Chinese, then all the text on the app should be in Chinese. 
- implement emoji jar related functions. B3
- Habit import/export function. H-2
- Emoji related functions: import/export function. E-1, E-2
- Card generation related functions: the final result should be a image file that can be shared to social media. it should include the selected text, background image, font style, font color, and the icon of the note. The card should be generated in a separate screen and can be previewed before saving and sharing. Once saved it will be stored in the card folder as image file. Add AI assistant to generate card design based on the content of the note and image - background and emoji etc select. AI should be able to generate multiple card designs for the user to choose from. 
