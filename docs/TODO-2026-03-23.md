Defer all Apple related work.

Continue Google Playstore App testing process. 
    The app link still not working as of this morning. 

Continue language checking for menu, diaglog etc. 
    1. Under the Summary, the title of analytics are Chinese for English setting;
    2. future: more locales can be added seamlessly

Next phase of improvements -  to be included in post beta along with any bugs need to be fixed from beta
    1. card edit and generation; requirement is simple to use but result is beautiful and powerful. So user want to share and save a keepsaker. Simple solution is having reload "card template" with background, frame, icon images etc plus user selected content (editable ) to allow manual card develop. the end result is an image (png for example) to be shared. Future solution: when AI is available, utlize AI image generation functionality and generate card-picture automatically. 
    2. AI scope filter. Add a system tag "Secretes" that user can select for notes. When user select this tag for the notes, the selected notes will be excluded from conversation content. 
    3. Identify sources for trial key - 7 days for example that can be loaded for user to try for free. 
    4. complete UX review - need identify tools and approaches. Figma? or any other AI tool
    